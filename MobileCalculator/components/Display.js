import React from "react";
import { Text, StyleSheet } from "react-native";

const Display = ({}) => {
	return (
		<>
			<Text>This will be the Display</Text>;
		</>
	);
};

const styles = StyleSheet.create({});
export default Display;
